import { CurrencyShortNameDirective } from './currency-short-name.directive';

describe('CurrencyShortNameDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrencyShortNameDirective();
    expect(directive).toBeTruthy();
  });
});
